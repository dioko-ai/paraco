{
  "credentials": { "demo-grant": "fake" },
  "routes": [
    {
      "selection": { "provider": "fake", "model": "small" },
      "credential": "demo-grant"
    }
  ],
  "apps": {
    "REPLACE_WITH_PARACO_IDENTITY_OUTPUT": { "credential_grants": ["demo-grant"] }
  },
  "default": { "provider": "fake", "model": "small" },
  "provider_default_models": { "fake": "small" }
}
